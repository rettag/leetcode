from setuptools import setup, find_packages

setup(
    name="your_project",
    version="0.1.0",
    author='Rett Graham',
    packages=find_packages(),
    url='https://github.com/rettag/leetcode',
    install_requires=[
        "pytest",
    ],
)
